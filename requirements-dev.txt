# Local development / CI tooling only - not installed in production
# (Docker image, Vercel serverless function). Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# Code Quality
black==23.11.0
flake8==6.1.0
mypy==1.7.1
